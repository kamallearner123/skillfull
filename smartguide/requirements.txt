fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# LLM inference — choose one based on hardware
# Option A: vLLM (requires CUDA, high-throughput GPU serving)
vllm>=0.5.0
xformers>=0.0.26
auto-gptq>=0.7.0
pypdf>=4.0.0

# Option B: llama-cpp-python (quantised GGUF, lower VRAM)
# llama-cpp-python>=0.2.77

# Hugging Face (for model downloads and tokenizers)
transformers>=4.41.0
huggingface-hub>=0.23.0

# Redis — conversation memory
redis>=5.0.0

# Async HTTP for SSE streaming helpers
httpx>=0.27.0
anyio>=4.3.0

# Structured output / JSON parsing
jsonschema>=4.22.0

# Utilities
python-dotenv>=1.0.0

# ── RAG / Vector DB ──────────────────────────────────────────────────────────
# ChromaDB — persistent local vector store (no separate server needed)
chromadb>=0.5.0

# Sentence-transformers — embedding model (CPU-efficient, ~80MB for MiniLM)
sentence-transformers>=2.7.0

# PyMuPDF — optional: extract text from PDF study materials for ingestion
PyMuPDF>=1.24.0
